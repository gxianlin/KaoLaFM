<android.support.v4.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/refresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.ilynn.kaolafm.ui.fragment.HomeFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal">

            <android.support.design.widget.TabLayout
                android:id="@+id/tabLayout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                app:tabIndicatorHeight="0dp"
                app:tabGravity="fill"
                app:tabMode="fixed"
                android:background="@color/white"
                app:tabSelectedTextColor="@color/text_color_1"
                app:tabTextColor="@color/title_tab_text"
                android:layout_weight="2"/>

            <com.ilynn.kaolafm.ui.custom.MyCheckBox
                android:id="@+id/checkbox"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@color/white"
                android:text="筛选"
                android:gravity="center"
                android:drawablePadding="5dp"
                android:textColor="@color/title_tab_text"
                android:drawableRight="@drawable/title_filtrate_icon"
                android:button="@null"/>
        </LinearLayout>

    </LinearLayout>

</android.support.v4.widget.SwipeRefreshLayout>
